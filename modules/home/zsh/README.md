# ZSH

ZSH options and additional `.zsh` imports. Writing extra configuration in `.zsh` files is preferred because it allows syntax highlighting and easier testing (you can source the files directly to test instead of rebuilding).